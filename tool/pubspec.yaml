name: tool
description: Validator, Simulator und Seed-Skript für Gustav.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  engine:
    path: ../packages/engine
  yaml: ^3.1.2
  args: ^2.5.0
  path: ^1.9.0

dev_dependencies:
  lints: ^4.0.0
