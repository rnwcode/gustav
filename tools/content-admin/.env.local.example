# Copy to .env.local and fill in real values (Supabase Dashboard → Project Settings → API).
# SUPABASE_SERVICE_ROLE_KEY bypasses RLS — this tool is local-only, never deployed
# (docs/specs/content-admin.md). Never prefix these with NEXT_PUBLIC_.
SUPABASE_URL=https://<ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
